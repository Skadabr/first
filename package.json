{
  "name": "first",
  "version": "0.0.1",
  "main": "server/src/index.js",
  "repository": "git@github.com:olegmedoed/first.git",
  "author": "Oleg Tsyba <oleg.tsyba.ua@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "docker-compose up -d --remove-orphans",
    "start:build": "docker-compose up -d --build --remove-orphans",
    "start:dev": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d --remove-orphans",
    "start:dev:build": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build --remove-orphans",
    "stop": "docker-compose down"

  }
}
